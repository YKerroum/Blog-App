<%= render @user %>

<div class="bio_container">
    <p> Bio </p>
    <p> <%= @user.bio %> </p>
</div>

<% @user.three_recent_posts.each_with_index do |post, i| %>
  <%= render partial: 'post', locals: { post: post, index: i} %>
<% end %>
